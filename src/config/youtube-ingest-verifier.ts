// Public verifier only. Never store the plaintext Apps Script INGEST_SECRET here.
// Set this to the 64-character lowercase SHA-256 hex digest of the secret.
// A strong random 40+ character secret makes publishing its SHA-256 verifier safe.
export const YOUTUBE_INGEST_SECRET_SHA256 = "";
